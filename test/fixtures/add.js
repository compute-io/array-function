'use strict';

/**
* FUNCTION: add( x, y )
*	Adds two values.
*
* @param {Number} x - input value
* @param {Number} y - input value
* @returns {Number} sum
*/
function add( x, y ) {
	return x + y;
}

// EXPORTS //

module.exports = add;
