'use strict';

/**
* FUNCTION: noop( val )
*	An array function which does not change an array element.
*
* @param {Number} val - array value
* @returns {Number} array value
*/
function noop( val ) {
	return val;
}

// EXPORTS //

module.exports = noop;
