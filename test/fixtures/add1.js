'use strict';

/**
* FUNCTION: add1( x )
*	Adds one to an input value.
*
* @param {Number} x - input value
* @returns {Number} sum
*/
function add1( x ) {
	return x + 1;
}

// EXPORTS //

module.exports = add1;
